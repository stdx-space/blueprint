[Unit]
Description=${package} restarter
After=network.target
StartLimitIntervalSec=10
StartLimitBurst=5

[Service]
Type=oneshot
ExecStart=/usr/bin/systemctl restart ${service}.service

[Install]
WantedBy=multi-user.target