tls {
  defaults {
    verify_incoming = true
    verify_outgoing = true
    %{ for key, value in tls_credentials ~}
    ${key} = "${value}"
    %{ endfor ~}
  }
  internal_rpc {
    verify_server_hostname = true
  }
}

auto_encrypt {
  ${tls_config_key} = true
}

ports {
  grpc_tls = 8503
}
